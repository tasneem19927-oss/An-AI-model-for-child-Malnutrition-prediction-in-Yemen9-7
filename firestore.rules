rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // ID Validation Helper to prevent path poisoning or Denial of Wallet attacks
    function isValidId(id) {
      return id is string && id.size() <= 128 && id.matches('^[a-zA-Z0-9_\\-]+$');
    }

    // Default safety catch-all rule (deny all)
    match /{document=**} {
      allow read, write: if false;
    }

    // Users Collection
    match /users/{userId} {
      allow get, write: if isValidId(userId);
      allow list: if true;
    }

    // Patients Collection
    match /patients/{patientId} {
      allow get, write: if isValidId(patientId);
      allow list: if true;
    }

    // Measurements Collection
    match /measurements/{measurementId} {
      allow get, write: if isValidId(measurementId);
      allow list: if true;
    }

    // Predictions Collection
    match /predictions/{predictionId} {
      allow get, write: if isValidId(predictionId);
      allow list: if true;
    }

    // Recommendations Collection
    match /recommendations/{recommendationId} {
      allow get, write: if isValidId(recommendationId);
      allow list: if true;
    }

    // Audit Logs Collection
    match /auditLogs/{auditLogId} {
      allow get, write: if isValidId(auditLogId);
      allow list: if true;
    }
  }
}
